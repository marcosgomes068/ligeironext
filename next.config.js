/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [],
  },
  // Desativa o estrito para desenvolvimento mais rápido
  reactStrictMode: false,
}

module.exports = nextConfig
